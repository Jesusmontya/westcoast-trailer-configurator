CHECKPOINT — West Coast Trailer Configurator + Landing Page

Proyecto: Dos entregables en un mes — (1) configurador 3D interactivo de trailers para West Coast LLC, y (2) reconstrucción completa de la página principal (westcoasttrailers.us). Jesús es socio/dueño del negocio y desarrollador.

Stack:

Configurador 3D: React + Vite + React Three Fiber (Three.js), carpeta frontend/
Página principal: Next.js (App Router, TypeScript, Tailwind CSS), carpeta website/
Backend: Python + FastAPI (pendiente de construir)
Base de datos: Supabase (PostgreSQL + RLS) (pendiente de construir)
IA: Google Gemini 2.5 Flash (para cotizaciones, pendiente)
Deploy: Vercel (página principal, ya configurado — Root Directory debe ser website) + Render/Netlify (configurador, pendiente)
Repo: GitHub — Jesusmontya/westcoast-trailer-configurator, monorepo con carpetas frontend/, website/, backend/, database/, docs/

Estado del CONFIGURADOR 3D (frontend/):

✅ Visor 3D funcional (React Three Fiber): trailer con paredes/techo transparentes (esmerilado), pared lateral izquierda más oscura como fondo visual, piso interior sólido
✅ Escena: carretera oscura con línea central amarilla segmentada, orientada correctamente en el eje del trailer, terreno alrededor
✅ Cámara: vista de frente al cargar (no en ángulo), rotación SOLO manual (sin auto-rotación)
✅ Panel de opciones "liquid glass": flotante, blur real, responsive (desktop = lado derecho, mobile = bottom sheet)
✅ Selector de 3 tamaños (12ft/16ft/20ft) que cambia dimensiones reales del modelo 3D
✅ Checklist de equipo interior (grill, freidora, fregadero, refrigerador, A/C, repisas) con posición fija cada uno
✅ Precio total en vivo (base del tamaño + suma de items seleccionados)
✅ Modelos: por ahora cajas geométricas simples (cuadrados), NO se usarán modelos glTF de bancos externos por peso/performance — se sustituirán más adelante por modelos reales del socio, optimizados con gltf-transform
⏳ Pendiente: drag & drop libre de los items dentro del trailer (decisión tomada: SÍ se quiere movimiento libre + colisiones — más complejo, identificado como riesgo de tiempo)
⏳ Pendiente: conectar a Supabase (guardar builds/leads)
⏳ Pendiente: cotización con Gemini + PDF

Estado de la PÁGINA PRINCIPAL (website/):

✅ Proyecto Next.js creado y corriendo local, deploy inicial en Vercel (con bug de Root Directory a corregir)
✅ Home base construido con Tailwind: hero oscuro, sección trust, testimonial (Annel/Pelons), CTA final — con bug de sintaxis JSX corregido (etiqueta <a> faltante)
Dirección de diseño definida (no genérica): lenguaje visual "liquid glass" consistente con el configurador — vidrio esmerilado en nav y tarjetas. Paleta: negro casi puro 
#0f0f10, blanco cálido 
#f5f5f0, rojo 
#e63946 (acento principal, ya usado en el configurador), amarillo 
#f1c40f (acento secundario, de la línea de carretera)
Hero decidido: minimalista, sin foto/video por ahora (se agrega después cuando haya footage real de clientes trabajando — decisión: SÍ se quiere pero hay que grabarlo/conseguirlo, 1-2 semanas de plazo estimado, no bloquea el resto)
Slogan elegido: "We build the trailer. You build the business."
Nav bar definido: sticky, vidrio, Logo | Home About Gallery Contact | ES/EN toggle | Build my trailer → (CTA en rojo sólido, resto en tono muted)
Idioma default: INGLÉS (para SEO/alcance), con toggle a español (mayoría real de clientes son latinos/paisas) — sistema de traducción simple con diccionario EN/ES, sin routing complejo
⏳ Pendiente: escribir el código final del nav + hero con estos requisitos
⏳ Pendiente: páginas About, Gallery, Contact
⏳ Pendiente: corregir Root Directory en Vercel (actualmente marca "Not Found")

Aprendizajes técnicos de esta sesión (para no repetir):

Cuidado al correr comandos de creación de carpetas — deben correrse desde la raíz correcta del repo para evitar anidamiento duplicado
npm create vite@latest . falla si la carpeta no está vacía — usar carpeta temporal (temp-vite) y copiar con cp -r temp-vite/. . en vez de mv
El GITHUB_TOKEN automático de Codespaces NO tiene permiso para crear repos vía gh repo create — hay que crear el repo desde la web de GitHub primero
Vercel necesita que el "Root Directory" apunte a la subcarpeta correcta (website) cuando el proyecto Next.js no vive en la raíz del repo

Precio de referencia de mercado (si se vendiera como servicio a terceros): Configuradores 3D con motor de precios: $3,000–$15,000 USD; con capa de IA y CRM completo: $10,000–$50,000 USD.





# CHECKPOINT — All Custom Trailers
**Fecha:** Agosto 2026

---

## 1. ARQUITECTURA

```
westcoast-trailer-configurator/  (nombre de repo sin cambiar, negocio ya rebrandeado)
├── website/    → Next.js, landing pública + panel admin
│   Dominios: allcustomtrailers.com, admin.allcustomtrailers.com
│   Vercel project: "website"
├── frontend/   → Vite + React, galería 3D de trailers escaneados
│   Dominio: 3d.allcustomtrailers.com
│   Vercel project: "frontend"
```

**Importante:** el custom-build (trailer a la medida) **se eliminó** de la galería. Ahora la galería es solo el visor 3D con su único formulario de interés.

---

## 2. BASE DE DATOS (Supabase)

### Tablas activas
- **`leads`** — con `status` (nuevo/contactado/cotizado/convertido/perdido), `converted_at`, `source`, `trailer_type`, `notes`, etc.
- **`lead_activity`** — historial de contacto por lead (nota + fecha)
- **`clients`** — ficha de cliente convertido: `quote_status`, `payment_type`, `total_amount`, `lead_id` de referencia
- **`payments`** — historial de abonos por cliente
- **`quote_presets`** — creada en SQL, **aún sin construir su pantalla** (plantillas de cotización con foto)

### Tablas legacy sin uso
- `builds`, `presets` (del configurador viejo, eliminado) — pendiente decidir si se borran

### SQL pendiente de correr (si no se ha hecho)
1. `nuevas_tablas.sql` → clients, payments, quote_presets
2. `migracion_leads_status.sql` → status, converted_at, lead_activity

---

## 3. PANEL ADMIN (admin.allcustomtrailers.com)

Ruta real: `website/app/panel-act-9k2m/`

| Archivo | Estado |
|---|---|
| `page.tsx` | ✅ entregado (v4) — header con tabs Leads / Clientes / PDF Generator |
| `LeadsPanel.tsx` | ✅ entregado — estados reales, urgencia +48h, historial de contacto, buscador |
| `ClientsPanel.tsx` | ✅ entregado — ficha de cliente, estado de pago, historial de abonos |
| `PdfGenerator.tsx` | ✅ entregado — genera cotización en PDF con membrete, **usuario no ha confirmado que probó el output** |

**Pendiente construir:** pantalla de **Plantillas de cotización** (`quote_presets`) — presets reutilizables con foto (de la galería 3D) + items con precio, para acelerar la generación de PDF.

---

## 4. GALERÍA 3D (frontend/)

| Archivo | Estado |
|---|---|
| `src/lib/supabase.js` | ✅ entregado — el frontend nunca tuvo cliente propio de Supabase |
| `src/lib/leads.js` | ✅ entregado — helper `submitLead()` con deduplicación (30 días por teléfono) |
| `src/components/gallery/TrailerModal.jsx` | ✅ entregado, ya conectado a `submitLead()`, source: `gallery` |
| `CustomBuildModal.jsx` | ❌ **ya no aplica** — se eliminó el custom-build de la galería, ignorar este archivo si se llegó a entregar |

**Pendiente para que funcione:**
- Agregar `VITE_SUPABASE_URL` y `VITE_SUPABASE_ANON_KEY` en Vercel (proyecto `frontend`) y `.env` local
- `npm install @supabase/supabase-js` dentro de `frontend/`
- Puede faltar la clase CSS `.modal-error` en `gallery.css`

---

## 5. DEDUPLICACIÓN DE LEADS

Vive en `submitLead()` (`website/lib/leads.ts` y `frontend/src/lib/leads.js`):
si ya existe un lead con el mismo teléfono en los últimos 30 días, no se duplica — se agrega como entrada nueva en `lead_activity`.

| Punto de entrada | ¿Usa `submitLead()`? |
|---|---|
| Galería 3D (`TrailerModal.jsx`) | ✅ sí |
| Formulario de contacto (landing) | ❌ **no todavía** — sigue insertando directo a `leads`, sin dedup. Pendiente si se quiere parejo. |

---

## 6. PENDIENTES GENERALES (fuera del sprint actual)

1. Confirmar que el generador de PDF funciona bien con datos reales
2. Construir pantalla de Plantillas de cotización (`quote_presets`)
3. Decidir si se actualiza el formulario de contacto de la landing para usar `submitLead()`
4. Fotos reales en el sitio (hero, about, gallery cards, testimonial) — siguen siendo placeholders
5. Más testimonios de clientes (el usuario dijo que los tiene, no los ha mandado)
6. Teléfono/email real en el footer (dice "coming soon")
7. Arreglar rotación chueca del modelo 3D escaneado (`rotationFix` en `mockTrailers.js`)
8. Decidir si se borran las tablas legacy `builds` y `presets`
9. Métrica de tiempo lead → cliente (ya se guarda `converted_at`, falta construir la vista/reporte)

---

## 7. FLUJO DE TRABAJO ESTÁNDAR

```bash
cd /workspaces/westcoast-trailer-configurator
# colocar archivos entregados en sus rutas
git add .
git commit -m "..."
git push
# auto-deploy en Vercel según qué carpeta cambió (website/ o frontend/)
```

Acceso al panel: **admin.allcustomtrailers.com** (login por Supabase Auth, Authentication → Users)



Repo: westcoast-trailer-configurator (monorepo) — dueño: Jesús Pavel Montoya Cerpa, fabricante de trailers personalizados, Nevada (cubre todo el estado, envía a cualquier lado, hace de todo tipo: comida, carga, utilidad).

westcoast-trailer-configurator/
├── website/    → Next.js: landing + admin panel (allcustomtrailers.com + admin.allcustomtrailers.com)
├── frontend/   → Vite+React: galería 3D (3d.allcustomtrailers.com)
DISEÑO — Paleta industrial (vigente, reemplazó la cálida de mostaza/cobre)
css
--bg: #eef1f3        /* gris acero claro */
--surface: #ffffff
--surface-2: #e2e6e9
--accent: #1f3a5c     /* azul marino — color de marca principal */
--accent-glow: #3d6690
--accent-2: #14263a   /* navy oscuro, hover */
--text: #1b1f23       /* casi negro */
--text-muted: #5b6570

Header/footer del sitio: fondo oscuro (var(--text)), texto blanco. Hero: video de fondo en loop (website/public/videos/hero-background.mp4) con overlay oscuro y texto blanco. Sin cuadrícula/grid decorativa (se quitó por petición explícita). Logo: todavía pendiente (prompt de diseño ya entregado al usuario para generarlo aparte).

SITIO PÚBLICO (website/)
app/
├── (site)/page.tsx          → landing: Hero(video)→Gallery→Advantages→Marquee→About→Process→Testimonial→Contact
├── components/Navbar.tsx, Footer.tsx    → paleta industrial
├── context/LanguageContext.tsx          → EN/ES
└── panel-act-9k2m/                      → admin (via middleware rewrite desde admin.*)

Formulario de contacto → submitLead() → RPC submit_client_lead → dedup por teléfono (30 días) → captura UTM (utm_source, utm_campaign) de la URL → guarda heard_from: "sitio_web_contacto".

Botón del hero hacia la galería 3D reenvía los parámetros UTM en el link.

GALERÍA 3D (frontend/)

Mismo patrón de dedup + UTM que la landing, heard_from: "sitio_web_galeria". Estilos en src/styles/gallery.css, recoloreados a la paleta industrial. TrailerModal.jsx usa clase .modal-error (ya definida).

PANEL ADMIN (website/app/panel-act-9k2m/)

Header: Clientes | Aceptadas | Catálogo | Ajustes | Log out

page.tsx — Lista de clientes:

Dashboard de stats (cotizado/aceptado/margen del mes, clientes nuevos)
Filtros: semáforo (🔴🟡🟢/sin definir), estado (activo/perdido), fecha, fuente
Semáforo de probabilidad (temperature: rojo="Veremos", amarillo="En proceso", verde="Aprobado") — clic cicla el color, tanto en lista como en ficha
Papelera con confirmación inline (no confirm() nativo)
"+ Nuevo cliente" con dedup automático por teléfono

clients/[id]/page.tsx — Ficha del cliente:

Editar info, toggle estado, semáforo, mover a papelera (todo con confirmación inline)
Seguimiento (notas con fecha)
QuoteSection: listar, Ver (preview con Editar), Duplicar, Descargar, Enviar por correo (Google Workspace vía nodemailer, marca emailed_at persistente), Borrar (con aviso si fallo)
QuoteBuilder: tamaño (con foto) → puerta en (pared) → agregar piezas a la pared (selector activo) → catálogo/presets con confirmación visual instantánea al agregar (check verde en la tarjeta) → barra fija de resumen ("X piezas · $Total") → lista de items con selector de pared por pieza → plano 2D agrupado por pared en Vista previa → Guardar cotización (ya NO descarga automático, separado de "Descargar")
Editar cotización existente actualiza el mismo registro (no duplica), borra el PDF viejo del Storage

catalog/page.tsx:

Tamaños (con largo real en pies, length_ft), Categorías, Piezas (con ancho en pulgadas width_in, activar/archivar, editar, importar CSV en lote, "Completar anchos" para piezas viejas sin ese dato), Presets (editable, recuerda acomodo)

settings/page.tsx (nuevo, separado de Catálogo): Tax %, puerta por default

quotes/page.tsx: Cotizaciones aceptadas — filtro mes/todo el tiempo, totales, margen (nota de que antes de agosto 2026 es aproximado)

BASE DE DATOS (Supabase) — tablas activas
clients            → +utm_source, +utm_campaign, +temperature (semáforo)
client_activity
quotes             → +emailed_at, +door_wall
catalog_categories
catalog_items      → +active, +width_in
catalog_presets, catalog_preset_items → +wall
trailer_sizes      → +length_ft
business_settings  → +default_door_wall
price_history
profiles           → role admin/ventas (solo Jesús tiene admin hoy)

Storage: catalog (fotos, público), quote-pdfs (privado)

Función clave: submit_client_lead(p_name, p_phone, p_email, p_interest, p_heard_from, p_utm_source, p_utm_campaign) — dedup 30 días

INTEGRACIONES EXTERNAS
Correo: Google Workspace vía SMTP/nodemailer (website/app/api/send-quote/route.ts), env vars GMAIL_USER + GMAIL_APP_PASSWORD en Vercel
PDF: generateQuotePdf.ts — diseño limpio sin colores (texto negro/gris), portada con foto del tamaño, campo download opcional para no forzar descarga
PENDIENTE / CONOCIDO
Fotos reales del negocio (trailers terminados, testimonios) — pospuesto a propósito, al final
Logo — prompt ya entregado, esperando que el usuario lo genere
Recolorear sitio/panel una vez que el logo esté listo (si cambia la paleta)
Ambiente de staging separado — no urgente aún (sin clientes reales en producción todavía)
Backups automáticos de Supabase — no configurado
2FA en el login — no configurado
Del análisis de 6 meses: límites de Supabase sin vigilar, listas sin paginar, lógica de leads duplicada entre website/ y frontend/, cero pruebas automáticas
El plano 2D es v1 — agrupa por pared con cajas simples, no dibuja a escala exacta todavía (se guarda width_in/length_ft para cuando se quiera afinar)
Falta probar el flujo completo de principio a fin con datos reales
RECOMENDACIÓN VIGENTE

Dejar de agregar features y usar el sistema una semana con datos reales (catálogo lleno, clientes reales, cotizaciones completas) antes de seguir construyendo — es la mejor señal de qué falta de verdad.




Claude is AI and can make mistakes. Please double-check responses.
